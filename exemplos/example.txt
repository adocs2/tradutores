int main () {
   int x;
   int s;
   int acce;
     forall(x in s) {
        if (!x) {
           int acc;
           acc = acc + x;
           acce = acc + x;
        }
        writeln(acce);
        writeln('a');
        writeln("opa");
     }
}