int main () {
    int j;
    set s;
    set x;
    int acce;
    int acc;
    int u;
    int y;
     forall(x in s) {
        if (!is_set(x)) {
           acc = acc + j;
           acce = acc + j;
           writeln("y");
           add(2 in add(1 in s));
        }
     }
     forall(x in 5) writeln('x');
     if(x in 5) writeln(u); else write(y);
    return 0;
}