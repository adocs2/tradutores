int main () {
    int x;
    set x;
     forall(x in s) {
        if (!is_set(x)) {
           acc = acc + x;
           acce = acc + x;
           writeln("y");
           add(2 in add(1 in s));
           x = x + 5;
        }
     }
     forall(x in 5) writeln('x');
     if(x in 5) writeln(u); else write(y);
    return 0;
}